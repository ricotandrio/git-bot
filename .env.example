# ============================================================
# GitBot — Discord × GitHub Projects Integration
# .env.example — Copy to .env and fill in your values
# ============================================================

NODE_ENV=development

# ────────────────────────────────────────
# DISCORD
# ────────────────────────────────────────

# Bot token from Discord Developer Portal → Your App → Bot → Token
DISCORD_BOT_TOKEN=

# Your Discord Application ID (OAuth2 → General → Application ID)
DISCORD_CLIENT_ID=

# The Guild (Server) ID where the bot is deployed
# Right-click your server icon → Copy Server ID (Developer Mode must be on)
DISCORD_GUILD_ID=

# Channel ID where daily standup posts will be sent
# Right-click the channel → Copy Channel ID
DISCORD_STANDUP_CHANNEL_ID=


# ────────────────────────────────────────
# GITHUB
# ────────────────────────────────────────

# Personal Access Token (classic) with scopes: repo, project
# GitHub → Settings → Developer Settings → Personal Access Tokens → Tokens (classic)
GITHUB_PAT=

# GitHub username or org that owns the target repository
GITHUB_OWNER=

# Target repository name (single repo for MVP)
GITHUB_REPO=

# GitHub Projects V2 — numeric project number (not the node ID)
# Found in the URL: github.com/orgs/<org>/projects/<NUMBER>
GITHUB_PROJECT_NUMBER=


# ────────────────────────────────────────
# SERVER
# ────────────────────────────────────────

# Port for the Express server
PORT=3000

# Environment: development | production
NODE_ENV=development


# ────────────────────────────────────────
# STORAGE
# ────────────────────────────────────────

# Storage driver for Discord ↔ GitHub user mapping
# Options: json | sqlite
STORAGE_DRIVER=sqlite

# Path to SQLite database file (used if STORAGE_DRIVER=sqlite)
SQLITE_DB_PATH=./data/gitbot.db

# Path to JSON mapping file (used if STORAGE_DRIVER=json)
JSON_MAPPING_PATH=./data/user-mapping.json


# ────────────────────────────────────────
# N8N (Automation / Scheduler)
# ────────────────────────────────────────

# Base URL of your n8n instance
N8N_BASE_URL=http://localhost:5678

# n8n webhook path for GitHub event triggers
N8N_WEBHOOK_PATH=/webhook/github-events

# API key for securing n8n webhook calls (set this in n8n as well)
N8N_WEBHOOK_SECRET=


# ────────────────────────────────────────
# GITHUB WEBHOOKS
# ────────────────────────────────────────

# Secret used to verify GitHub webhook payloads
# GitHub → Repo → Settings → Webhooks → Secret
GITHUB_WEBHOOK_SECRET=


# ────────────────────────────────────────
# CACHE
# ────────────────────────────────────────

# /board cache TTL in seconds (default: 60)
BOARD_CACHE_TTL=60
